import { save, load, remove, clear } from "./Storages";
import { formatPhone, replaceBlank } from "./String";

export default {
  save,
  load,
  remove,
  clear,
  formatPhone,
  replaceBlank,
};
